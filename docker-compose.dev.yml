name: datc-development

networks:
  datc-network:
    driver: bridge

services:
  mongodb:
    image: mongo:6.0
    networks:
      - datc-network
  server:
    build:
      context: .
      dockerfile: ./apps/server/dockerfile.dev
    container_name: server
    ports:
      - '4000:4000'
    networks:
      - datc-network
